#include<stdio.h>

int fact(int);

int main()
{
    int x = 5;
    printf("Factorial of %d is: %d", x, fact(x));
    return 0;
}

int fact(int i)
{
    if(i <= 1)
        return 1;
    else
        return i * fact(i - 1);
}

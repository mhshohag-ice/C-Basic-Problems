#include<stdio.h>

int main()
{
    int num,r,sum=0;

    printf("Enter a number: ");
    scanf("%d",&num);

    while(num){
        r=num%10;
        sum=sum+r;
        num=num/10;
    }

    printf("Sum of digits is %d", sum);

    return 0;
}
